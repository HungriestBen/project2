    <div class="gap"></div>
<div class="create_card_placeholder"></div>
    <div class="gap"></div>

<div class="latest_cards">
    <h2>Latest Cards</h2>
    <div class="gap"></div>
    <div class="latest_cards_grid">
        <% cards.each do |card| %>

        <h3>
            <a href="cards/<%= card["id"] %>">
            <%= card["title"] %></a>
            <img src="<%= card["image_url"] %>" alt="">
        </h3>

        

        <%end%>
    </div>
</div>